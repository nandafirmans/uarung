@{
    var isUseDefaultLayout = ViewContext.ActionDescriptor.FilterDescriptors
        .All(f => f.Filter.GetType() != typeof(Uarung.Web.Utility.RemoveDefaultLayout));

    if (isUseDefaultLayout)
    {
        Layout = "_Layout";
    }
}
